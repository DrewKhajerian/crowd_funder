<h1>My Projects</h1>

<ul>
	<% @projects.each do |project| %>
		<li class="project">
		  <%= link_to project.title, project %>
		  <br />
		  <p>
		    <%= project.teaser %>
		  </p>
		</li>
	<% end %>
</ul>
